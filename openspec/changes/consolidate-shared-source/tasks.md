# Tasks — Consolidate shared source

## Windows work-unit evidence (historical)

- Task 3 is partial: PR 1a commit `6f86bfde68400ab737a273f555b1a15277e12fb0` is based on `a200152bab52e2a2020d02498127b3ce4ac9797d` with Conventional Commit message `feat(release): add inventory-only schema two validation`.
- The work has 361 authored additions + deletions across `scripts/release/matrix.py`, `tests/test_release_matrix_schema2.py`, and `apply-progress.md`.
- An independent verifier reproduced 16 passing tests plus rejection of 10 mutations and confirmed the baseline live matrix hash was unchanged; a CRLF-versus-stored-LF false alarm was resolved.
- Native review is unavailable: two consent attempts expired without lineage or mutation, and no native approval is claimed.
- No push or PR was performed. All 17 tasks remain unchecked, with task 3 partial. Remaining work is recorded in [`apply-progress.md`](apply-progress.md).

## Mac continuation — verified local foundations

- Tasks2 and3 are complete locally. Normalized consumers, bootstrap transitions, selected Gradle contexts, Stonecutter source adapters, serial-runner primitives and scoped artifact verification/staging have progressed in bounded conventional commits; exact changes and limitations are in [apply-progress.md](apply-progress.md).
- At `be54c78`, the broad Mac suites pass:252 Python tests (3 opt-in Gradle smoke tests skipped here and passed separately) and216 CI tests. Real strict Fabric/Forge builds pass both default and isolated dispatch; all four JARs retain baseline hashes. These remain dirty diagnostic builds, without packaged client E2E or clean release provenance.
- The live matrix now uses schema2/preparing with twelve targets and three configured lanes: both existing1.20.1 rows plus guarded Fabric1.21.1. Original `.gitignore`/`.pi` bytes remain preserved. Normalized consumers and schema3 staging require explicit scope; modern Stonecutter game execution remains guarded. Synthetic detached-source probes are not Minecraft lane qualification.
- Task7 is implemented and verified locally: real clean-copy serial builds, observed toolchains, schema3 staging and a failing Java-test stop pass in preparing fixtures. Tasks4–6 and11–12 have additional local slices; embedded identities, declared overlays and scoped workflow/fan-in dispatch are implemented. The three loader common-path changes follow exact contract-first then next/collapse commits. Twelve real lane configurations/ports, packaged qualification, shared activation, AUTH-1 deployment and separately authorized sync retirement remain open. No publication or remote mutation occurred.
- The later `task7j` schema1 and `task7k` preparing-schema2 clean-copy runs supersede the earlier dirty-build limitation for those two lanes' compilation/provenance only. Schema3 explicitly remains partial2/12; a separate Broken.java run stops before Forge. Detailed commit/tree/digest evidence is in apply-progress; cross-checkout archive byte equality is not assumed. Task8 enrollment is complete locally:301 Python tests (3 opt-in Gradle skips) and290 CI tests pass, while default dispatch remains legacy2 and ten targets remain unresolved.

- The first canonical source seam (ResourceLocation construction) is implemented with exact argument preservation and real Stonecutter/javac API probes. A clean `b15fdf8` serial build/staging passes both legacy lanes after the port; Fabric1.21.1 still requires dependency closure, further source/metadata ports and client qualification. Pages/visual and durable-build readers have further bounded adapters; no release/publication authority is inferred.
- The later clean `98f1a0a` integrated check passes340 Python tests (4 opt-in Gradle skips) and303 CI tests. Raw/compact Pages APIs preserve explicit partial coverage and reject file substitution; Build transports a separately sealed report, and the graph reader accepts caller-owned scope without narrowing PR/scheduled gates. Remaining freshness/authority adapters are not inferred from these structural passes.

- Independent closure review marks tasks4 and11 complete as local implementation: normalized consumers and scoped archive/provenance rules are implemented and tested. This does not close task9 configuration, task15 twelve-lane packaged qualification or AUTH-1 delivery.

- Independent acceptance review also closes tasks5/6 as local foundations: exact current/next loader transitions and isolated pinned Stonecutter/Loom/source validation are implemented. The clean `3a29d5c` checkpoint passes379 Python tests (7 real-Gradle opt-ins skipped in that suite) and308 CI tests. Real networking API/backend probes pass separately; no game qualification or delivery is implied.

- Through `8868ec5`, clean legacy evidence at `40f36c9` covers both production/harness builds after codecs, Gecko imports and interaction adapters; root verifies all 724 source inputs and staged reports unchanged. ItemStack component bridging passes separate native Java17/21 probes. Release acquisition now joins current producer metadata and four authenticated ZIPs with content checks, while scoped Pages assembly retains explicit coverage and external selection bindings. These are local slices of tasks10/12/13: no release CLI, complete modern compilation, twelve-lane gameplay qualification or delivery is claimed. The tracked apply-progress document records exact commits and limitations; user work and publication restrictions remain preserved.

- Through `f8d39b2`, task13 is complete as local implementation: the canonical-only CLI binds current producer/API/ZIP/content evidence and emits only an E2E-tested production lane plan with explicit partial coverage; root repeats24 tests and independent adversarial review closes late source/default drift. This grants no deployment or publication authority. Box/Figure/Claw persistence, destruction and item-renderer/tooltip API probes pass separately on Java17/21. The isolated `c8ae2b7` full modern diagnostic captures136 errors without truncation; later bounded source units are outside that checkpoint. Pages collect now emits attempt-bound companions, while acquisition/assembly/refresh/rotate wiring remains open. User files and sync retirement restrictions remain preserved.

- Through `3c92a67`, a new clean legacy production/harness/check and staging run passes both Fabric/Forge1.20.1, with remapped mixin packaging independently inspected and756 source inputs unchanged. Gifting, color-widget, renderer, star-quad and local-profile capture units have separate native17/21 evidence; complete modern compilation and client gameplay remain open. Pages build/refresh now consume authenticated attempt companions, while rotation/discovery adapters remain pending. All local commits stay within400 authored lines, user files remain preserved, and no publication authority is inferred.

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | 4,500–7,500 additions + deletions across source, Gradle, Python, CI, tests, and docs |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR 0 authorization gate → PR 1 schema readers → PR 2 consumer adapters → PR 3 bootstrap/foundation → PR 4 serial runner → repeated lane/seam/artifact/CI slices → activation → retirement |
| Delivery strategy | auto-chain |
| Chain strategy | stacked-to-main |

Decision needed before published admission: Yes
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: High

## Delivery boundaries

- Integration is the locally evidenced `master`; `stacked-to-main` describes stacked PR shape only and does not rename `master`.
- Each numbered implementation unit is one proposed conventional work-unit commit and one stacked slice against the then-current `master`; after integration, rebase and re-evaluate the next slice. Do not create branches, commits, PRs, pushes, publications, or chain artifacts during planning.
- Each slice must stay at or below 400 authored additions + deletions, including its tests and documentation. If a cohesive unit exceeds that after this one slicing plan, stop and request `size:exception`; none is authorized.
- AUTH-1 blocks initial foundation delivery and every later published restricted-path admission; it does not block the user's authorized unpublished local implementation, tests, configuration, or conventional work-unit commits on the existing feature branch. Local readiness does not create authority, alter a deployed evaluator, or permit a workaround. A clean committed checkout/CI is required later for release-grade staging; existing dirty `.gitignore` and `.pi` work must be retained and never stashed, reset, or committed.
- Strict TDD is unconfirmed. Use proportional tests for each runnable surface and packaged-E2E acceptance for gameplay; record the exact command, environment, scope, and result. The inherited 136-test result (2 failures, 38 errors) and Gradle class-major-69 failure are baseline limitations, not passing verification.

## Tasks

- [ ] 1. **Delivery gate — establish and prove the foundation before published restricted admission.** Use [`authorization-plan.md`](authorization-plan.md) to guide local development and review of the minimal controller foundation, but do not let candidate policy authorize its own deployment. Before delivery, obtain fresh governance evidence and explicit human authority for the exact reviewed foundation commit and action; after admission, independently prove that generation active on the exact live default branch. Then require authenticated base/controller generation, exact-head owner decisions, schema/graph evidence, and rollback boundaries for later restricted transitions. **Edit scope:** local foundation code/tests/docs may be implemented under the existing migration and commit authorization; this draft grants no push, PR, merge, default-branch update, publication, release, or protection change. **Depends on:** none. **Verification/acceptance:** evidence covers foundation self-admission prevention and the future bounded routes for `release/release-matrix.json`, `gradle/verification-metadata.xml`, both `VERSION_SPECIFIC_PATHS`, Stonecutter admission, and NeoForge bootstrap without disabling protections or reusing a bypass. Keep task 1 unchecked until the foundation is deployed and observed active; its pending state does not block authorized unpublished local tasks. **Rollback:** revise local candidate work normally; revoke or roll back deployed authority only through a fresh reviewed and explicitly authorized action, never reset or context disabling. **Estimate:** foundation slices remain subject to the 400-line cap.

- [x] 2. **Gate — establish usable verification environments without altering user work.** In an authorized local or later clean checkout/CI, select an installed JDK 21 before Gradle and record `java -version`, `gradlew --version`, toolchain availability, `git status --short`, and fresh remote/governance probe results. **Edit scope:** no tracked-file edits; environment/process configuration and evidence only. **Depends on:** none for local observation; task 1 before delivery reliance. **Verification/acceptance:** distinguish Java 21 Gradle launch results from the known baseline; retain live `master` SHA and API limitations, and never treat 403-limited protection/ruleset evidence as proof of absence. **Rollback:** remove only temporary clean-checkout/CI outputs. **Estimate:** 0–40 lines of authorized evidence/configuration.

- [x] 3. **PR 1 — add schema-1/schema-2 normalized matrix parsing without changing the live matrix.** Extend `scripts/release/matrix.py` and focused portability fixtures in `tests/test_release_matrix_portability.py` to dispatch schemas exactly, preserve schema-1 bytes/behavior, validate the twelve `targets`, `migration` state, lane routing/toolchain/repository fields, and report unresolved configurations explicitly. **Edit scope:** `scripts/release/matrix.py`, `tests/test_release_matrix_portability.py`, and test fixtures under `tests/`; do not edit `release/release-matrix.json`. **Depends on:** none for the local test-first parser subunit; task 1 before eventual delivery because `scripts/release/` is a future governed surface. **Verification/acceptance:** run the focused portability test and `python scripts/release/matrix.py --matrix release/release-matrix.json`; mutations reject missing, duplicate, extra, mixed-era, and unknown-schema data while current schema-1 output remains unchanged. **Rollback:** revert only parser/tests/fixtures in this slice. **Estimate:** 260–380 lines.

- [x] 4. **PR 2 (repeat per consumer) — adapt one normalized matrix consumer at a time.** Update one concrete consumer from `settings.gradle`, `build.gradle`, `scripts/release/artifact_manifest.py`, `scripts/release/verify_release.py`, `e2e/orchestrator.py`, `e2e/runtime_store.py`, or `scripts/ci/e2e_job_graph.py`, plus its nearest existing test module, to consume a validated normalized lane projection rather than a separate inventory. **Edit scope:** exactly one consumer, `scripts/release/matrix.py` only if its projection API needs a bounded addition, and that consumer’s tests; exclude live matrix activation. **Depends on:** task 3 for local implementation; task 1 before delivery of governed roots. **Verification/acceptance:** prove schema-1 compatibility and reject an absent, inconsistent, or cross-lane projection; retain a manifest/runtime/CI consumer test for the selected consumer. **Rollback:** revert the one consumer, its projection addition, and tests together. **Estimate:** 180–360 lines per repeated slice.

- [x] 5. **PR 3 (repeat per bootstrap contract) — prepare current/next loader-bootstrap validation before changing executable bytes.** Update `scripts/ci/loader_bootstrap.py` and focused tests in `scripts/ci/tests/test_loader_bootstrap.py` for one documented current/next transition generation, preserving the old contract as current and rejecting stale, mixed, or unauthorized bytes. **Edit scope:** `scripts/ci/loader_bootstrap.py`, its tests, and one locally prepared transition record whose later delivery remains under AUTH-1; do not edit `common/src/e2e/java/com/theplumteam/e2e/VanillaShim.java` or `e2e/server-template/datapack/pack.mcmeta` in this slice. **Depends on:** task 3 where schema-dependent for local implementation; task 1 before delivery. **Verification/acceptance:** run the focused bootstrap tests and `python scripts/ci/loader_bootstrap.py --repository . --head-sha <exact-committed-sha>` in an eligible checkout; negative cases fail closed. **Rollback:** revert validator/tests/authorized transition record as one generation, retaining the prior contract. **Estimate:** 180–340 lines per contract generation.

- [x] 6. **PR 4 — add the Stonecutter compatibility probe and selected-node foundation without activation.** Develop locally the minimal pinned Stonecutter integration at `stonecutter.gradle` and selected-node wiring in `settings.gradle`/`build.gradle`, with narrowly scoped dependency-policy/repository admission changes in `gradle/repository-policy.gradle`, `gradle/verification-metadata.xml`, `scripts/ci/dependency_policy.py`, and corresponding tests; published admission remains under AUTH-1. **Edit scope:** those exact Gradle/policy files and their tests; no live matrix schema switch, source relocation, or broad plugin/dependency upgrade. **Depends on:** tasks 3–4 and JDK-21 evidence from task 2 for local implementation; task 1 before delivery. **Verification/acceptance:** a per-lane configuration probe materializes one selected Fabric, Forge, or NeoForge context, rejects missing/conflicting selectors, and does not configure mixed FML families; checksum/repository negative tests remain effective. **Rollback:** revert the complete integration/policy/test slice, leaving existing single-era build paths intact. **Estimate:** 300–400 lines; split repository-policy closure from controller wiring if the measured diff approaches 400.

- [x] 7. **PR 5 — implement serial matrix planning, then lock/process execution as separate slices.** First add planning to `scripts/release/build_matrix.py` with `tests/test_build_matrix.py`; then, in a separate slice, add one-checkout locking, isolated Gradle homes, serial subprocess execution, atomic reports, cancellation cleanup, and stale-success invalidation. **Edit scope:** `scripts/release/build_matrix.py`, `tests/test_build_matrix.py`, and bounded documentation in `docs/operations.md` only when its command contract changes. **Depends on:** tasks 3–4 and task 6 for local implementation; task 1 before delivery. **Verification/acceptance:** mocked tests prove numeric-version/loader order, `--no-daemon --no-parallel --max-workers=1`, no concurrent Gradle process, lock contention behavior, process failure/cancellation cleanup, wrong JDK/parallel override rejection, and changed matrix/source invalidation. **Rollback:** revert planner or executor as independently usable slices; never kill unrelated user processes. **Estimate:** 220–360 lines per slice.

- [x] 8. **PR 6 — enroll schema-2 preparation with all twelve target identities and only existing executable rows.** Change `release/release-matrix.json` to schema 2, `migration.mode: preparing`, all twelve exact targets, and the existing two executable 1.20.1 rows; update only the already-adapted matrix tests and projections required to preserve legacy dispatch. **Edit scope:** `release/release-matrix.json`, `scripts/release/matrix.py`, and focused matrix/consumer tests; AUTH-1 governs later published admission. **Depends on:** tasks 3–7 for local implementation; task 1 before delivery. **Verification/acceptance:** target projection is exactly twelve; executable/runtime/artifact rows remain two and report the other targets unresolved; legacy build and harness dispatch are unchanged and no target is claimed qualified or published. **Rollback:** revert live-matrix enrollment and directly dependent projections together to schema 1. **Estimate:** 220–360 lines.

- [ ] 9. **PR 7 (repeat once per lane) — add complete lane inputs and isolated node checks.** For one target at a time, add its reviewed matrix configuration and source-route declarations in `release/release-matrix.json`, selected-node build wiring in `settings.gradle`/`build.gradle` and relevant `{fabric,forge,neoforge}/build.gradle`, plus portability/configuration tests. **Edit scope:** the named matrix, selected-node Gradle files, one loader build file, and focused tests; no unrelated lane configuration. **Depends on:** tasks 6 and 8 for local implementation; task 1 before delivery. **Verification/acceptance:** the lane has explicit rather than inferred versions, Java/toolchain, repositories, task/output paths, and source routes; selected-node probe rejects cross-family context and reports an unresolved/missing input rather than fallback. **Rollback:** revert that lane’s configuration/wiring/tests without changing other target membership. **Estimate:** 180–380 lines per lane; perform all twelve lanes, retaining 1.20.1 Fabric/Forge and 1.21.1/1.21.4/1.21.5/1.21.6/1.21.7 Fabric/NeoForge.

- [ ] 10. **PR 8 (repeat by compatibility seam) — reconcile historical behavior into canonical sources and narrow overlays.** Before each port, compare only the documented historical SHA/path with current canonical paths; migrate one behavior seam (entrypoint/metadata, identifiers/networking, blocks/items/data, renderer/UI, or harness adapter) into `common/src/main`, `common/src/e2e`, and the applicable `{fabric,forge,neoforge}/src/{main,e2e}` path, using only matrix-declared `src/legacy<era>` overlays. **Edit scope:** one named seam’s canonical and loader/overlay paths, its test or packaged-E2E scenario contract, and a bounded reconciliation note in the applicable documentation; no wholesale historical checkout, snapshot copy, or unrelated Quick-Skin-Mod code. **Depends on:** the affected task-9 lane configuration for local implementation; task 1 before delivering governed E2E/bootstrap paths. **Verification/acceptance:** record source SHA/path, retained behavior, explicit incompatibility and lane scope; verify registry IDs, packet/data semantics, and applicable deterministic scenario against the real production interaction. **Rollback:** revert only that seam/overlay and its scenario/test documentation. **Estimate:** 150–390 lines per seam; split large screens/renderers by behavior rather than deleting tests or comments.

- [x] 11. **PR 9 (repeat per artifact/provenance rule) — evolve staging to scoped schema-3 manifests and hard artifact separation.** Adapt `scripts/release/artifact_manifest.py`, `scripts/release/verify_release.py`, artifact tests in `tests/test_artifact_and_report_validation.py`, and the selected Gradle/harness convention path so each lane emits separate production and harness archives with lane/matrix/tree/contract provenance. **Edit scope:** one manifest/verifier rule group, its tests, and the directly needed archive convention; preserve schema-2 reader support and exclude release selection changes until task 13. **Depends on:** tasks 4, 7, and 9 for local implementation; task 1 before delivery. **Verification/acceptance:** mutations reject cross-lane pairing, stale matrix/tree/contract identity, harness leakage (including nested content) into production, production content in harness, symlink/special/unlisted staged files, and partial scope presented as full. **Rollback:** revert the schema-3 rule and its producer/consumer/test change together, retaining schema-2 legacy staging. **Estimate:** 220–390 lines per rule group.

- [ ] 12. **PR 10 (repeat per E2E/CI consumer) — derive lane runtime, CI, and packaged-E2E coverage from the matrix.** Update one consumer among `e2e/orchestrator.py`, `e2e/runtime_store.py`, `e2e/scenario-contract.json`, `.github/workflows/build-gate.yml`, `.github/workflows/on-demand-e2e.yml`, `scripts/ci/e2e_job_graph.py`, `scripts/ci/e2e_fanin.py`, and their tests, only after its reader is compatible. **Edit scope:** one consumer/contract or one workflow-graph expectation plus matching test files; do not weaken trusted contexts, artifact authentication, scenario assertions, or advisory/authoritative separation. **Depends on:** tasks 4, 5, 9, and 11 for local implementation; task 1 before delivery. **Verification/acceptance:** all declared configured lanes receive matching production/harness/runtime/CI/scenario identities; tests reject skipped lanes, stale/mixed/partial evidence, wrong-lane harnesses, and candidate-controlled graph changes. **Rollback:** revert the one consumer and tests, retaining prior trusted gate behavior. **Estimate:** 200–390 lines per consumer slice.

- [x] 13. **PR 11 — add independent qualified-lane release planning without publication.** Add `scripts/release/plan_release.py`, `tests/test_release_selection.py`, and the minimal manifest/matrix projection support needed for `--artifact-node`; update `docs/release-architecture.md` with the no-publication command and evidence requirements. **Edit scope:** those files and bounded supporting release modules/tests; no publish workflow or remote release action. **Depends on:** tasks 4, 9, and 11 for local implementation; task 1 before delivery. **Verification/acceptance:** a selected qualified lane derives its own version and production-only identity while another lane remains unresolved; missing, stale, mismatched, or failed evidence fails closed and a lane plan never marks the migration complete. **Rollback:** revert planner/projection/tests/docs together. **Estimate:** 260–390 lines.

- [ ] 14. **PR 12 (repeat per loader transition) — prepare, test, and later deliver each exact-next bootstrap generation.** Change one locally authorized loader/bootstrap surface among `common/src/e2e/java/com/theplumteam/e2e/VanillaShim.java`, `e2e/server-template/datapack/pack.mcmeta`, loader entrypoints, and `e2e/loader-bootstrap-contract.json`; then collapse current/next acceptance only after exact-next checks pass. **Edit scope:** exactly one loader/contract transition and its focused validator tests, as named by AUTH-1; no generic hash refresh. **Depends on:** tasks 5 and 9–12 for local implementation; task 1 before the authorized transition is delivered. **Verification/acceptance:** current+next validation covers the behavioral change, the final contract has a single exact binding, and old/stale/unauthorized bytes fail. **Rollback:** revert the executable change and its contract generation through the authorized route. **Estimate:** 140–350 lines per loader transition.

- [ ] 15. **Gate — complete per-lane production, harness, boundary, and packaged-E2E qualification.** In authorized clean CI/checkouts, run the serial runner and staged verifier for each of the twelve matrix lanes, then execute its matching real packaged production JAR and harness scenarios; capture lane-specific matrix/tree/toolchain/artifact/report identities. **Edit scope:** no source mutation unless a separately planned failed slice is reopened; generated `build/` outputs only. **Depends on:** tasks 7–14 for every applicable lane. **Verification/acceptance:** all twelve lanes pass matrix validation, serialized production/harness builds, archive boundary/provenance checks, loader metadata/data smoke checks, and deterministic real interaction assertions; unavailable/failing lanes remain visible and deny global completion. **Rollback:** delete only generated diagnostic outputs; preserve historical refs and prior evidence. **Estimate:** 0–120 tracked lines for any authorized qualification report, with runtime cost outside diff budget.

- [ ] 16. **PR 13 — activate shared mode only after every configured path is qualified.** Switch `release/release-matrix.json` from `preparing` to `shared`, replace only already-proven legacy execution projections in `settings.gradle`, `build.gradle`, `scripts/release/matrix.py`, and relevant aggregation/CI consumers, and update operations/release/E2E documentation. **Edit scope:** matrix activation plus directly dependent projection/aggregate/docs files; no new source port, policy weakening, or sync retirement. **Depends on:** tasks 2–15; task 1 and fresh governance facts before delivery. **Verification/acceptance:** full plans contain exactly twelve build/harness/runtime/CI/E2E lanes; direct Gradle aggregates route users to the serial runner; no legacy fallback occurs on a failing lane; trusted protection tests still pass. **Rollback:** reviewed authorized revert to `preparing` with legacy dispatch preserved; do not reset user work. **Estimate:** 280–400 lines; split aggregate routing from documentation if measured over budget.

- [ ] 17. **Gate/PR 14 — retire release synchronization only under separate authorization.** After shared-mode qualification, equivalent replacement-protection evidence, fresh remote/default-branch facts, and an explicit retirement authorization, make the smallest cohesive change to `.github/workflows/sync-release-branches.yml` and associated `scripts/ci`/documentation/tests. **Edit scope:** only the separately authorized retirement paths; preserve historical refs, tags, and release evidence. **Depends on:** tasks 1–16 plus independent retirement authorization and fresh remote/governance evidence. **Verification/acceptance:** prove all twelve lanes and replacement trusted protections are current; absent authority or remote evidence leaves the workflow untouched; security/graph tests reject weakened authentication or skipped lanes. **Rollback:** restore the reviewed workflow/controller generation through the authorized route and revalidate before any release dispatch. **Estimate:** 180–380 lines; no publication is included.
